#include <stdio.h>
#include <malloc.h>
#include "treeNode.h"


/*void IniciarTree(Node **Root){
    *Root = NULL;
}

void insercao(Node **pRoot, int valor2)
{
	if (*pRoot == NULL)
	{
	      *pRoot=(Node *)malloc(sizeof (Node));
	      (*pRoot)->esquerda=NULL;
	      (*pRoot)->direita=NULL;
	      (*pRoot)->valor=valor2;
	}
	else 
        {
		if (valor2 < ((*pRoot)->valor))
		{
			insercao(&((*pRoot)->esquerda), valor2);
		}
		else
		{
			insercao(&((*pRoot)->direita), valor2);
		}
	}
}
*/
int main(void) {
  printf("Hello World\n");
  return 0;
}